<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a10118429_ruyatsyah">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.10118429_ruyatsyah">
        <activity android:name=".walkthorough.WalkthroughActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".catatanharian.KategoriActivity" />
        <activity android:name=".catatanharian.ListNoteActivity" />
        <activity android:name=".catatanharian.NotesActivity" />
        <activity android:name=".catatanharian.SaveActivity" />
        <activity android:name=".catatanharian.SaveKategoriActivity" />
        <activity android:name=".dashboard.DashboardActivity" />
        <activity android:name=".catatanharian.SplashScreen" />
    </application>

</manifest>